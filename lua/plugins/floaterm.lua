return {
  "voldikss/vim-floaterm",
  config = function()
    -- 这里可以设置一些Floaterm的默认行为，暂时保持默认即可
    -- 例如，你可以设置浮动窗口的大小、位置等
    -- vim.g.floaterm_width = 0.8
    -- vim.g.floaterm_height = 0.8
  end
}
